[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn app:app --workers 4 --timeout 300 --bind 0.0.0.0:$PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"

[variables]
NIXPACKS_APT_PKGS = "ffmpeg"
